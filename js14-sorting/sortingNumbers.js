const numbers = [3, 50, 1, 4, 9, 7, 3, 2, 1];

numbers.sort(); // para ordenar strings
numbers.sort((a, b) => a - b); // esta coisa estranha para ordernar numeros, vamos aprender com detalhe mais tarde

console.log(numbers);